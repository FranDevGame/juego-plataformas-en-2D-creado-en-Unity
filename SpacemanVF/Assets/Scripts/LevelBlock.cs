using UnityEngine;

public class LevelBlock : MonoBehaviour
{
    public Transform startPoint, exitPoint; /*Declaramos dos variables public "Transform"
                                             *siendo Transform el tipo de dato que se encarga
                                             *de saber posición, rotación y escala. En Mates, 
                                             *son las transformaciones afines del espacio y con
                                             *lo que se genera los universos 2D como 3D.
                                             *//*Hemos declarado dos variables del mismo tipo de dato
                                             * tan solo poniendo una coma entre el nombre de una y otra.*/



    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
